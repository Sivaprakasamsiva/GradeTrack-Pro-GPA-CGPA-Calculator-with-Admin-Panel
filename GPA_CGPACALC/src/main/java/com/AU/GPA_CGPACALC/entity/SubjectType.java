package com.AU.GPA_CGPACALC.entity;

public enum SubjectType {
    CORE,
    LAB,
    ELECTIVE,
    THEORY,     // optional: if your frontend uses strings like "THEORY"
    PRACTICAL   // optional: keep if you map these strings on frontend
}
